[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sandblaster"
version = "1.0.0"

[project.scripts]
ssay = "sandblaster.__main__:main"

[tool.setuptools.packages.find]
where = ["."]